#ifndef VALIDINPUT_H
    #define VALIDINPUT_H
    #include <stdbool.h>
    
    void CommandLineValidInput(int rows, int columns, int pieces);
    bool isValidInput(int num_args_needed, int num_args_read);

#endif